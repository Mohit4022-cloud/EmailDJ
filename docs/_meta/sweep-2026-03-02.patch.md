# DocOps Sweep — 2026-03-02

Sweep mode: full audit + Tier 1 regeneration + missing canonical doc creation.

---

## Summary Table

| File | Action | Tier | Notes |
|---|---|---|---|
| `docs/_meta/docmap.yaml` | **Updated** | 1 | Rewritten from JSON coverage_rules schema to canonical YAML `version/bindings` format. Added bindings for all new docs. |
| `docs/_meta/glossary.md` | **Updated** | 1 | Expanded from 11 to 28 terms. Added: session, mode, preset, context_vault, model_cascade, SSE, token_vault, PII_prefilter, Presidio, side_panel, service_worker, delegation_engine, hub_api, gold_set, judge_pipeline, freshness_check, doc_rot. |
| `docs/contracts/openapi.md` | **Created** (renamed from `openapi_summary.md`) | 1 | Canonical doc name per skill spec. Added How-it's-generated header and Related Docs links. Content preserved from `openapi_summary.md`. |
| `docs/contracts/openapi_summary.md` | **Deleted** | — | Replaced by `docs/contracts/openapi.md`. |
| `docs/contracts/schemas.md` | **Created** | 1 | Full field-level documentation for all 25+ Pydantic models in `hub-api/api/schemas.py`. Columns: field / type / required / description. |
| `docs/policy/prompt_contracts.md` | **Created** | 2 | All 6 prompt builder functions documented. 9 non-negotiable constraints enumerated with code refs. Output format contract specified. Preset strategy table included. |
| `docs/ops/release_checklist.md` | **Created** | 2 | 4-phase staged rollout checklist. Pre-release gates from CI + lock compliance + judge eval. Rollback procedure. Copy-paste PR checklist. |
| `docs/product/presets.md` | **Created** | 2 | Canonical engineering-facing preset doc. 6 canonical presets, 10 alias mapping, slider defaults table, hook type definitions, governance rules. |
| `docs/ops/env_matrix.md` | **Updated** | 1 | Columns rewritten to canonical format: `VAR_NAME / default / required? / used by / safe test value / notes`. Reorganized by section (LLM Providers, Redis, CRM, etc.). Added `required?` and `safe test value` columns. All 77+ vars preserved. |
| `hub-api/scripts/doc_freshness_check.py` | **Created** | infra | Reads `docmap.yaml`. Diffs `base...head`. Fails if bound code changed without doc change. CLI: `--base`, `--head`, `--warn-only`. Handles `on_change` (blocking) and `on_major_change` (advisory). |
| `.github/workflows/ci.yml` | **Updated** | infra | Added standalone `doc-freshness` job that runs `hub-api/scripts/doc_freshness_check.py` in parallel with the `checks` job. PR context: blocking. Push/schedule: warn-only. |

---

## Tier 2/3 Items Requiring Human Review

The following docs were **auto-drafted** and are marked `<!-- AUTO-DRAFTED: review before merge -->`.
A human must review them for accuracy before treating them as source of truth:

### `docs/contracts/schemas.md` (Tier 1 — low risk, but verify field descriptions)
- Verify `WebPreviewBatchMeta` observability fields are complete and descriptions accurate
- Confirm `WebhookEditRequest` / `WebhookSendRequest` / `WebhookReplyRequest` descriptions

### `docs/policy/prompt_contracts.md` (Tier 2 — review carefully)
- Constraint #4 ("strict grounding"): confirm that `claim_verifier.py` is actually integrated
  into the web MVP prompt path, not just the quick-generate path
- Constraint #8 (research injection safety): verify this is enforced in `output_enforcement.py`
  or only at model-instruction level
- `get_master_brief_prompt` and `get_persona_angle_prompt`: output contracts are undocumented
  (stubs in code) — add output contract once implemented

### `docs/ops/release_checklist.md` (Tier 2 — review phase criteria)
- Phase 3.2 Real Mode Canary: verify the exact `real_mode_smoke.py` pass criteria
- Phase 4.1 Nightly gates: verify threshold numbers match current `calibration/latest.json`

### `docs/product/presets.md` (Tier 2 — review alias mapping)
- Alias 6 resolves to `giver` and alias 7/10 to `industry_insider`: confirm this is the
  intended UX mapping or if aliases 6–10 should eventually map to NEW presets
- The "Icebreaker", "Trusted Advisor", "Visionary", "VIP Pass" presets exist in
  `docs/EmailDJ SDR Presets.md` but have NO corresponding `PresetStrategy` in code.
  **Decision needed:** add them to `preset_strategies.py` or remove from product docs.

---

## ADR Candidates (no ADRs created by this sweep)

| Candidate | Context |
|---|---|
| ADR-0002: Prompt template versioning | `prompt_templates.py` has no version field. When prompts change, there is no audit trail beyond git blame. Recommend adding a `PROMPT_VERSION` constant and stamping it in SSE done metadata. |
| ADR-0003: Preset alias resolution for aliases 6–10 | Aliases 6, 7, 9, 10 resolve to existing presets. If the product intends to ship 10 distinct presets, the missing 4 (`icebreaker`, `trusted_advisor`, `visionary`, `vip_pass`) need to be implemented in code. |
| ADR-0004: `docs/adr/` vs `docs/_meta/adr/` location | Skill spec specifies `docs/_meta/adr/` but actual ADRs live in `docs/adr/`. This sweep preserves `docs/adr/` (correct, since the CI references it). Formally accept `docs/adr/` as the canonical location. |

---

## How to Run Freshness Check Locally

```bash
# Full freshness check (compare working tree against main)
python hub-api/scripts/doc_freshness_check.py --base origin/main --head HEAD

# Warn-only (advisory, no exit 1)
python hub-api/scripts/doc_freshness_check.py --base origin/main --head HEAD --warn-only

# After a PR is merged, verify all generated docs are fresh
python scripts/docops/generate_docs.py --check

# Regenerate Tier 1 docs (env matrix + OpenAPI summary)
python hub-api/scripts/generate_openapi.py   # regenerates hub-api/openapi.json
python scripts/docops/generate_docs.py       # regenerates docs/ops/env_matrix.md, docs/contracts/openapi.md
```

---

## Structural Gaps Detected (not fixed by this sweep)

| Gap | Location | Severity |
|---|---|---|
| `docs/architecture/overview.md` Mermaid diagram may be stale | `docs/architecture/overview.md` | low — bound to `on_major_change`, no structural changes this sweep |
| `docs/ops/runbooks.md` RB-05 (cost guard) references `MONTHLY_COST_THROTTLE_MULTIPLIER` — verify logic still matches `api/middleware/cost_guard.py` | `docs/ops/runbooks.md` | low |
| 4 product presets (Icebreaker, Trusted Advisor, Visionary, VIP Pass) have no code implementation | `hub-api/email_generation/preset_strategies.py` | medium — product gap |
