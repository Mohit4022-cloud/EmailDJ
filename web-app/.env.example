# Required for deployed builds. In local dev, the client falls back to http://127.0.0.1:8000.
VITE_HUB_URL=http://127.0.0.1:8000

# Mirror the backend preview gate. Set to on only when EMAILDJ_PRESET_PREVIEW_PIPELINE=on server-side.
VITE_PRESET_PREVIEW_PIPELINE=off
